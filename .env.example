# ===========================================
# Qwen Chat - Environment Variables
# ===========================================
# Copy this file to .env and adjust values
# cp .env.example .env

# -------------------------------------------
# Backend (Express Server)
# -------------------------------------------

# Port for the Express server
PORT=3001

# Node environment (development, production, test)
NODE_ENV=development

# -------------------------------------------
# Ollama API Configuration
# -------------------------------------------

# Ollama API base URL (default: http://localhost:11434)
OLLAMA_API_URL=http://localhost:11434

# Default model to use (can be changed in UI)
OLLAMA_DEFAULT_MODEL=qwen3:8b

# -------------------------------------------
# CORS Configuration
# -------------------------------------------

# Comma-separated list of allowed origins
# Default allows Vite dev server and preview server
CORS_ORIGINS=http://localhost:5173,http://localhost:4173

# -------------------------------------------
# Frontend (Vite - must be prefixed with VITE_)
# -------------------------------------------

# API URL for frontend to connect to backend
VITE_API_URL=http://localhost:3001

# WebSocket URL (usually same as API URL)
VITE_WS_URL=http://localhost:3001

# App name (displayed in UI)
VITE_APP_NAME=Qwen Chat

# Version (auto-set from package.json in production)
VITE_APP_VERSION=0.1.0
